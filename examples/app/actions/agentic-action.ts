// import { GraphRunnable } from 'ts-edge';

// export type AgenticActionContext<Runnable extends GraphRunnable> = {
//   runner: Runnable;
// };

// export const agenticAction = <Runnable extends GraphRunnable>({runner}: AgenticActionContext<Runnable>) => {
//     runner.getStructure()[0].

//   return {
//     execute(input: Parameters<Runnable['run']>[0]) {
//       return runner.run(input);
//     },
//   };
// };
